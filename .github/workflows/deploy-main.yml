name: Deploy Encodec API

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  packages: read

concurrency:
  group: deploy-encodec-api-main
  cancel-in-progress: true

env:
  ENCODEC_API_CLUSTER_LABEL: wavey
  ENCODEC_API_NAMESPACE: encodec-api
  ENCODEC_API_DOMAIN: encodec.wavey.ai
  ENCODEC_API_INGRESS_IMAGE_REPO: ghcr.io/wavey-ai/encodec-api-ingress
  ENCODEC_API_WORKER_IMAGE_REPO: ghcr.io/wavey-ai/encodec-api-worker
  ENCODEC_API_KUSTOMIZE_PATH: deploy/k8s/encodec-api
  ENCODEC_API_REGISTRY: ghcr.io

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Configure git auth for private dependencies
        run: |
          set -euo pipefail
          git config --global url."https://x-access-token:${{ secrets.WAVEY_AI_GH_TOKEN }}@github.com/".insteadOf https://github.com/

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up kubectl
        uses: azure/setup-kubectl@v4

      - name: Validate deploy artifacts
        run: |
          set -euo pipefail
          cargo metadata --locked --no-deps >/dev/null
          python3 -m py_compile deploy/linode_api.py
          bash -n deploy/lke/manual-deploy.sh
          kubectl kustomize "$ENCODEC_API_KUSTOMIZE_PATH" >/dev/null

      - name: Detect image changes
        id: changes
        run: |
          set -euo pipefail

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "wait_image=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          before="${{ github.event.before }}"
          if [ "$before" = "0000000000000000000000000000000000000000" ]; then
            before=""
          fi

          if [ -n "$before" ] && git cat-file -e "$before^{commit}" 2>/dev/null; then
            changed_files="$(git diff --name-only "$before" "$GITHUB_SHA")"
          elif git rev-parse --verify HEAD^ >/dev/null 2>&1; then
            changed_files="$(git diff --name-only HEAD^ HEAD)"
          else
            changed_files="$(git ls-files)"
          fi

          if [ -z "$changed_files" ]; then
            changed_files="$(git show --pretty='' --name-only "$GITHUB_SHA")"
          fi

          wait_image=false
          while IFS= read -r path; do
            [ -n "$path" ] || continue
            case "$path" in
              .github/workflows/build-image.yml|.dockerignore|docker/*|docker/**|src/*|src/**|Cargo.toml|Cargo.lock)
                wait_image=true
                ;;
            esac
          done <<<"$changed_files"

          echo "wait_image=$wait_image" >> "$GITHUB_OUTPUT"

      - name: Wait for image build workflow
        if: steps.changes.outputs.wait_image == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          import os
          import sys
          import time
          import urllib.request

          repo = os.environ["GITHUB_REPOSITORY"]
          sha = os.environ["GITHUB_SHA"]
          token = os.environ["GH_TOKEN"]
          url = (
              f"https://api.github.com/repos/{repo}/actions/workflows/build-image.yml/runs"
              f"?head_sha={sha}&event=push&per_page=10"
          )
          headers = {
              "Authorization": f"Bearer {token}",
              "Accept": "application/vnd.github+json",
              "X-GitHub-Api-Version": "2022-11-28",
          }
          deadline = time.time() + 45 * 60

          while time.time() < deadline:
              req = urllib.request.Request(url, headers=headers)
              with urllib.request.urlopen(req, timeout=30) as resp:
                  payload = json.load(resp)
              runs = payload.get("workflow_runs", [])
              if runs:
                  run = runs[0]
                  status = run.get("status")
                  conclusion = run.get("conclusion")
                  print(f"build-image.yml run {run.get('id')} status={status} conclusion={conclusion}")
                  if status == "completed":
                      if conclusion == "success":
                          sys.exit(0)
                      sys.exit(1)
              time.sleep(15)

          sys.exit(1)
          PY

      - name: Resolve LKE cluster
        id: cluster
        env:
          LINODE_TOKEN: ${{ secrets.LINODE_TOKEN }}
        run: |
          set -euo pipefail
          python3 deploy/linode_api.py cluster-id \
            --label "$ENCODEC_API_CLUSTER_LABEL" \
            --github-output "$GITHUB_OUTPUT"

      - name: Fetch cluster kubeconfig
        id: kubeconfig
        env:
          LINODE_TOKEN: ${{ secrets.LINODE_TOKEN }}
        run: |
          set -euo pipefail
          kubeconfig_path="$RUNNER_TEMP/wavey-kubeconfig.yaml"
          python3 deploy/linode_api.py cluster-kubeconfig \
            --cluster-id "${{ steps.cluster.outputs.cluster_id }}" \
            --output "$kubeconfig_path" \
            --github-output "$GITHUB_OUTPUT"

      - name: Deploy encodec-api to LKE
        env:
          KUBECONFIG: ${{ steps.kubeconfig.outputs.kubeconfig }}
          LINODE_TOKEN: ${{ secrets.LINODE_TOKEN }}
          REGISTRY_SERVER: ${{ env.ENCODEC_API_REGISTRY }}
          REGISTRY_USERNAME: jbrough
          REGISTRY_PASSWORD: ${{ secrets.WAVEY_AI_GH_TOKEN }}
          ENCODEC_API_INGRESS_REPLICAS: ${{ vars.ENCODEC_API_INGRESS_REPLICAS || '1' }}
          ENCODEC_API_WORKER_REPLICAS: ${{ vars.ENCODEC_API_WORKER_REPLICAS || '1' }}
        run: |
          set -euo pipefail

          kubectl label nodes -l node.kubernetes.io/instance-type=g2-gpu-rtx4000a1-s \
            nvidia.com/gpu.present=true \
            --overwrite || true

          ingress_image="${ENCODEC_API_INGRESS_IMAGE_REPO}:main"
          worker_image="${ENCODEC_API_WORKER_IMAGE_REPO}:main"
          if [ "${{ steps.changes.outputs.wait_image }}" = "true" ]; then
            short_sha="${GITHUB_SHA::7}"
            ingress_image="${ENCODEC_API_INGRESS_IMAGE_REPO}:sha-${short_sha}"
            worker_image="${ENCODEC_API_WORKER_IMAGE_REPO}:sha-${short_sha}"
          fi

          echo "${{ github.token }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

          wait_for_image() {
            local image="$1"
            local attempts="${2:-60}"
            local delay_seconds="${3:-10}"
            for _ in $(seq 1 "$attempts"); do
              if docker manifest inspect "$image" >/dev/null 2>&1; then
                return 0
              fi
              sleep "$delay_seconds"
            done
            echo "Timed out waiting for image $image" >&2
            return 1
          }

          wait_for_image "$ingress_image"
          wait_for_image "$worker_image"

          ENCODEC_API_INGRESS_IMAGE="$ingress_image" \
          ENCODEC_API_WORKER_IMAGE="$worker_image" \
          bash deploy/lke/manual-deploy.sh
